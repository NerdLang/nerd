var fn = function(_arg, a2, a3, a4)
{
	var args = arguments();

	for(var i = 0; i < length_of(args); i++)
	{
		{{
			std::cout << args[i] << std::endl;
		}}
	}
	return {
		a: _arg,
	};
};

fn();

fn("a", "b", "c", "d", "e");